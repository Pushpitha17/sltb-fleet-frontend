import { revalidatePath } from "next/cache"

export async function selectFilter(
  prevState: {
    message: string
  },
  formData: FormData
) {
  try {
  } catch (error) {
    console.log(error)
  }
}
